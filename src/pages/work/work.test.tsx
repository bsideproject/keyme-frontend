import { render, fireEvent, screen } from "@testing-library/react";

import Work from "./work";

describe("업무 관리 화면 테스트", () => {
  it("업무 관리 렌더링 테스트", () => {
    render(<Work />);

    // To Do List
  });
});
